<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:NASAGallery.ViewModels"
             x:Class="NASAGallery.MainPage">
    <ContentPage.BindingContext>
        <local:MainPageViewModel />
    </ContentPage.BindingContext>

    <StackLayout>
        <!-- Place new controls here -->
        <Button HorizontalOptions="Fill" Text="Astronomy Picture of the Day" Command="{Binding GotoApodCommand}"/>
        <Button HorizontalOptions="Fill" Text="NASA Image and Video Library" Command="{Binding GotoSearchCommand}"/>
        <Button HorizontalOptions="Fill" Text="Test API" Command="{Binding TestApiCommand}"/>
    </StackLayout>

</ContentPage>
